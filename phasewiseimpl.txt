
3. Then develop it using small prompts

After adding the instruction file, your prompts to the coding agent become much simpler:

Read instructions.md.

Implement Phase 1.

Create the Spring Boot Maven project with Java 21 and dependencies needed for:
- Spring Web
- Spring Validation
- Spring AI OpenAI
- Kafka
- Redis
- PostgreSQL
- Actuator
- Testing

Do not implement business logic yet.

Create application.yml with environment-variable placeholders.

Explain every dependency you add.

Then:

Read instructions.md.

Implement Phase 2: WhatsApp webhook integration.

Create:
- WebhookController
- WhatsAppWebhookRequest DTOs
- InboundMessageService
- WhatsAppMessage domain model

Support:
1. webhook verification
2. incoming text messages
3. payload validation
4. correlation ID generation
5. logging

Do not integrate Kafka yet.

Also provide curl requests that can simulate WhatsApp webhook calls locally.

Then Kafka:

Read instructions.md.

Implement Phase 3.

Add Kafka-based asynchronous processing.

Flow:

WhatsAppWebhookController
    -> InboundMessageService
    -> Kafka Producer
    -> whatsapp.inbound
    -> Kafka Consumer

Create a WhatsAppMessageEvent record.

Add:
- producer configuration
- consumer configuration
- serialization
- retry handling
- DLT configuration

Do not integrate the LLM yet.

Add unit tests.

Then the important AI part:

Read instructions.md.

Implement Phase 4.

Integrate Spring AI with OpenAI.

Create:

AIOrchestrator
ChatService
PromptService

Kafka consumer should call AIOrchestrator.

For now use:

System Prompt
+ User Message
-> LLM
-> Response

Do NOT add RAG or Redis memory yet.

Externalize model name, temperature and API configuration.

Add logging for:
- request correlation ID
- model
- processing duration

Do not log secrets or sensitive message content.
Yes. Below is a complete **Phase 4 onward prompt sequence** you can give to Copilot/Cursor/Claude one phase at a time.

I’d use these in order: **LLM → Memory → RAG → Tools → WhatsApp response → Reliability → Guardrails → Token optimization → Observability → Docker/testing → Evaluation.**

## Phase 4 — Spring AI + OpenAI + AI Orchestrator

```text
Read instructions.md and inspect the existing project before making changes.

Implement Phase 4: Spring AI + OpenAI LLM integration.

Goal:
Consume an inbound WhatsApp message from Kafka and generate an AI response using Spring AI.

Flow:

Kafka Consumer
    -> AIOrchestrator
    -> PromptService
    -> Spring AI ChatClient
    -> OpenAI
    -> AI Response

Implement:

1. AIOrchestrator
   - Main orchestration layer for AI processing.
   - Accept the normalized inbound message.
   - Build the AI request.
   - Invoke the LLM.
   - Return the generated response.

2. PromptService
   - Keep prompt construction separate from orchestration.
   - Create a system prompt for a customer-support assistant.
   - Pass the customer message as the user prompt.

3. Spring AI ChatClient configuration.

4. Externalized configuration for:
   - API key
   - model name
   - temperature
   - max tokens where supported

5. Kafka consumer integration:
   Kafka Consumer -> AIOrchestrator

6. Error handling for:
   - OpenAI timeout
   - API errors
   - invalid/empty responses

7. Logging:
   - correlationId
   - model
   - processing duration
   - success/failure

Never log API keys or full sensitive customer data.

8. Unit tests for AIOrchestrator and PromptService.

Do NOT implement:
- RAG
- Redis conversation memory
- tool calling
- advanced guardrails

Explain:
- ChatClient
- system vs user prompt
- temperature
- why AIOrchestrator exists
- how Spring AI communicates with the model

Provide all modified files and application.yml configuration.
```

---

## Phase 5 — Redis Conversation Memory

```text
Read instructions.md and existing Phase 4 implementation.

Implement Phase 5: Redis-backed conversational memory.

Goal:
Allow the AI agent to remember recent conversation context for each WhatsApp customer.

Flow:

Customer Message
    -> AIOrchestrator
    -> Load Conversation Memory from Redis
    -> PromptService
    -> LLM
    -> Store User + Assistant Messages
    -> Redis

Implement:

1. ConversationMemoryService.

2. Redis-backed implementation.

Use a key pattern similar to:

conversation:{customerId}

3. Store conversation turns containing:
   - role
   - content
   - timestamp

4. Maintain bounded history.
Do not keep unlimited conversation history.

Make configurable:
   - maximum number of messages
   - Redis TTL

5. AIOrchestrator should:
   - load previous history
   - include relevant history in the LLM request
   - invoke LLM
   - save the new user message
   - save the assistant response

6. Configure Redis serialization properly.

7. Handle:
   - missing conversation
   - expired conversation
   - Redis failure

8. Add unit tests.

Explain:
- Why Redis is suitable for conversation memory
- Redis vs database
- TTL
- bounded history
- how conversation memory affects token usage

Do NOT implement RAG yet.
```

---

## Phase 6 — RAG

This is one of the **most important phases** for your project.

```text
Read instructions.md and existing implementation.

Implement Phase 6: Retrieval-Augmented Generation using Spring AI.

Goal:
Answer customer questions using information retrieved from our own knowledge base rather than relying only on the LLM's general knowledge.

Architecture:

Knowledge Documents
    -> Document Loader
    -> Text Chunking
    -> Embedding Model
    -> Vector Store

Runtime:

Customer Question
    -> AIOrchestrator
    -> RAG Service
    -> Similarity Search
    -> Relevant Chunks
    -> Prompt Context
    -> LLM
    -> Response

Implement:

1. KnowledgeBaseIngestionService.

Load sample customer-support documents from:

src/main/resources/knowledge/

Create sample documents for:
- refund policy
- order cancellation
- delivery policy
- payment FAQ

2. Split documents into appropriate chunks.

Explain the selected chunk size and overlap.

3. Generate embeddings using Spring AI embedding support.

4. Store embeddings in a vector store supported by the current project.

For the prototype, prefer the simplest practical option.
If PostgreSQL already exists, consider PGVector.

5. Create RagService.

Expose a method conceptually similar to:

retrieveRelevantContext(String query)

6. Perform semantic similarity search.

Make configurable:
- topK
- similarity threshold

7. Integrate RAG with AIOrchestrator.

Final flow:

User Message
    -> Conversation Memory
    -> RAG Retrieval
    -> Prompt Construction
    -> LLM

8. PromptService should clearly separate:

SYSTEM INSTRUCTIONS
CONVERSATION HISTORY
RETRIEVED KNOWLEDGE
USER QUESTION

9. Tell the model:
- Use retrieved context when answering business-specific questions.
- Do not invent policies.
- If reliable information is unavailable, say that the information could not be found.

10. Add tests for:
- relevant document retrieval
- no relevant document
- multiple retrieved chunks
- empty knowledge base

Explain:
- embeddings
- vector databases
- semantic search
- chunking
- topK
- similarity score
- RAG vs fine-tuning
- how RAG reduces hallucination
```

---

# Phase 7 — Spring AI Advisors

Because your resume specifically mentions **Spring AI Advisors**, implement this separately rather than pretending normal RAG is automatically an Advisor implementation.

```text
Read the current project and Phase 6 RAG implementation.

Implement Phase 7: Spring AI Advisor-based RAG integration.

Goal:
Use Spring AI Advisors to modularize retrieval/context augmentation around ChatClient calls.

First inspect the Spring AI version used by this project and use APIs supported by that exact version.
Do not invent deprecated Advisor APIs.

Implement:

1. Configure the appropriate Spring AI RAG / vector-store Advisor.

2. Integrate the VectorStore with the Advisor.

3. Configure retrieval parameters:
   - topK
   - similarity threshold

4. Refactor ChatClient configuration so RAG augmentation is handled through the Advisor where appropriate.

5. Keep AIOrchestrator responsible for orchestration rather than low-level vector-search implementation.

6. Compare:

Manual RAG:
AIOrchestrator
 -> RagService
 -> VectorStore
 -> Prompt
 -> LLM

Advisor-based RAG:
AIOrchestrator
 -> ChatClient
 -> Advisor
 -> VectorStore
 -> augmented prompt
 -> LLM

7. Add logging showing:
   - whether retrieval happened
   - number of documents retrieved
   - retrieval duration

Do not log full confidential document contents.

8. Add tests.

Explain:
- What a Spring AI Advisor is
- why Advisors are useful
- Advisor chain/interception concept
- manual RAG vs Advisor-based RAG
```

---

# Phase 8 — Tool / Function Calling

This gives you a strong interview story because RAG and tool calling solve **different problems**.

```text
Read instructions.md and the current implementation.

Implement Phase 8: Spring AI tool/function calling.

Goal:
Allow the LLM to execute controlled business operations instead of hallucinating dynamic information.

Create a mock Order Status capability.

Example:

User:
"Where is my order ORD-123?"

LLM determines that order information is required.

LLM
    -> OrderStatusTool
    -> OrderService
    -> Mock Repository
    -> structured result
    -> LLM
    -> natural-language response

Implement:

1. OrderService.

2. Mock order repository with sample orders.

3. OrderStatusTool using the tool/function calling mechanism supported by the current Spring AI version.

Input:

orderId

Output:

orderId
status
estimatedDeliveryDate

4. Register the tool with ChatClient.

5. Allow the LLM to decide when the tool is required.

6. Validate tool arguments.

7. Do not allow arbitrary method execution.

8. Add timeout and error handling around tool execution.

9. Log:
   - tool name
   - execution duration
   - success/failure

10. Add tests.

Explain:
- tool calling
- function calling
- RAG vs tool calling

Example distinction:

"What is your refund policy?"
-> RAG

"Where is order ORD-123?"
-> Tool call

"Cancel order ORD-123"
-> Tool/action, with additional authorization required in a real production system.
```

---

# Phase 9 — Structured Output

```text
Read the current project.

Implement Phase 9: Structured LLM outputs.

Goal:
Avoid relying on unstructured text when downstream application logic needs predictable data.

Create an AI response model such as:

AiResponse
- answer
- category
- confidence
- requiresHumanSupport

Possible categories:

ORDER
PAYMENT
REFUND
DELIVERY
GENERAL

Use the structured-output capability supported by the current Spring AI version.

Requirements:

1. Map LLM responses into a Java record/class.

2. Validate generated structured output.

3. Handle parsing failures safely.

4. Add fallback behavior if structured output cannot be generated.

5. AIOrchestrator should work with the structured result.

6. WhatsApp-facing response should primarily use the answer field.

7. If requiresHumanSupport=true, prepare the system for future human escalation.

Add tests for:
- valid structured output
- malformed output
- unknown category
- empty answer

Explain why structured outputs are preferable to manually parsing arbitrary LLM text.
```

---

# Phase 10 — WhatsApp Outbound Response

```text
Read instructions.md and existing project.

Implement Phase 10: Send AI-generated responses back to WhatsApp.

Flow:

Kafka inbound
 -> AIOrchestrator
 -> AI Response
 -> WhatsAppResponseService
 -> WhatsApp Cloud API
 -> Customer

Implement:

1. WhatsAppClient interface.

2. WhatsApp Cloud API implementation.

3. WhatsAppResponseService.

4. Request DTOs required by WhatsApp API.

5. Externalized configuration:
   - base URL
   - phone number ID
   - access token

6. Never hard-code tokens.

7. Add:
   - HTTP timeout
   - error handling
   - safe logging

8. Distinguish retryable and non-retryable HTTP errors.

9. Add a mock implementation/profile so the complete application can run locally without sending real WhatsApp messages.

10. Add tests.

Explain the outbound WhatsApp flow and authentication mechanism.
```

---

# Phase 11 — Idempotency + Duplicate Protection

This is important because webhook/event systems commonly provide **at-least-once delivery**.

```text
Read the current project.

Implement Phase 11: Idempotency and duplicate WhatsApp event protection.

Problem:

The same WhatsApp webhook message may be delivered multiple times.

We must avoid:

same message
 -> Kafka
 -> LLM
 -> response

multiple times.

Implement:

1. IdempotencyService backed by Redis.

2. Use WhatsApp messageId as the primary idempotency key.

Example:

idempotency:whatsapp:{messageId}

3. Before publishing/processing an event:

check Redis.

If already processed:
    log duplicate
    skip processing

Otherwise:
    safely claim/process the event.

4. Use TTL so keys do not remain forever.

5. Consider concurrency:

Two application instances may receive the same message simultaneously.

Use an atomic Redis operation such as SET NX rather than:

GET
then
SET

which has a race condition.

6. Explain whether the idempotency record should be created:
   - before processing
   - after processing
   - or using processing/completed states

Choose an appropriate approach for this prototype.

7. Add tests for:
   - first event
   - duplicate
   - simultaneous duplicate
   - TTL expiration
   - processing failure

Explain:
- idempotency
- at-least-once delivery
- duplicate delivery
- race condition
- why Redis SET NX helps
```

---

# Phase 12 — Kafka Reliability + Retry + DLT

```text
Read the current Kafka implementation.

Implement Phase 12: Production-style Kafka failure handling.

Implement:

1. Retry strategy.

2. Exponential or fixed backoff appropriate for the prototype.

3. Dead Letter Topic:

whatsapp.dlt

4. Configure Spring Kafka DefaultErrorHandler or the appropriate error handling mechanism for the project's Spring Kafka version.

5. Differentiate retryable errors:

- temporary OpenAI failure
- temporary network failure
- temporary downstream service failure

from non-retryable errors:

- invalid payload
- validation failure
- permanently unsupported event

6. Preserve:
   - correlationId
   - messageId
   - original event information

when sending to DLT.

7. Avoid infinite retries.

8. Explain offset commit behavior and what happens when processing fails.

9. Add tests.

Explain:
- retry
- backoff
- DLT
- consumer offset
- poison message
- at-least-once processing
```

---

# Phase 13 — Rate Limiting

```text
Read the existing project.

Implement Phase 13: Rate limiting.

Goal:
Protect the application and expensive LLM APIs from excessive requests.

Implement a Redis-backed rate limiter appropriate for a multi-instance application.

Apply limits per customer/customerId.

Example:

10 AI requests per minute per customer.

Requirements:

1. RateLimitService.

2. Configurable:
   - request limit
   - time window

3. Apply rate limiting before expensive LLM processing.

4. Return a friendly response when the limit is exceeded.

5. Ensure multiple application instances share rate-limit state through Redis.

6. Add tests.

Explain:
- fixed window
- sliding window
- token bucket

Choose one approach for this prototype and explain why.
```

---

# Phase 14 — Guardrails + Prompt Injection

```text
Read instructions.md and existing AI implementation.

Implement Phase 14: AI guardrails and validation.

Goal:
Reduce unsafe, irrelevant, manipulated, and hallucinated responses.

Implement an input/output guardrail layer.

Input checks:

1. Null/empty input.
2. Maximum message length.
3. Unsupported message type.
4. Basic prompt-injection indicators.

Examples:

"Ignore all previous instructions"

"Reveal your system prompt"

"Print your hidden instructions"

5. Treat retrieved RAG documents and user content as untrusted data.

6. Ensure user-provided content cannot replace system-level instructions.

Output checks:

1. Empty LLM output.
2. Invalid structured output.
3. Excessively long response.
4. Business-specific claims unsupported by available context where practical.

5. Provide safe fallback responses.

6. Do not claim that simple regex detection makes the application fully secure against prompt injection.

Create appropriate abstractions such as:

InputGuardrail
OutputGuardrail
GuardrailService

Integrate them into AIOrchestrator.

Add tests for normal and malicious inputs.

Explain:
- prompt injection
- direct vs indirect prompt injection
- input guardrails
- output guardrails
- limitations of guardrails
```

---

# Phase 15 — Token Optimization / Context Engineering

This phase helps support your resume statement about reducing token consumption.

```text
Read the existing AI orchestration, memory and RAG implementation.

Implement Phase 15: Context and token optimization.

Goal:
Reduce unnecessary tokens while preserving response quality.

Analyze the current prompt sent to the LLM.

Optimize:

1. Conversation history.

Do not send unlimited history.

Keep recent relevant turns.

2. RAG.

Do not send excessive retrieved documents.

Use:
- topK
- similarity threshold
- bounded context size

3. System prompt.

Remove unnecessary repetitive instructions.

4. Tool definitions.

Expose only tools needed for the application.

5. Add token usage tracking if supported by the model/provider response metadata.

Capture where available:

inputTokens
outputTokens
totalTokens

6. Log aggregated/token metadata safely.

7. Create an abstraction such as ContextBuilder responsible for assembling:

system instructions
+ conversation history
+ RAG context
+ user message

within configured limits.

8. Do not use naive character count as an exact token count.

9. Add tests.

Explain how:
- conversation history
- retrieved chunks
- system prompts
- tool schemas

contribute to token consumption.

Show before/after examples of the context assembled for the same request.
```

---

# Phase 16 — Hallucination Reduction + Fallback

```text
Read the existing project.

Implement Phase 16: Hallucination reduction and fallback handling.

Goal:
Prevent the assistant from confidently inventing customer-support policies or transaction information.

Implement:

1. For knowledge/policy questions:
   Prefer retrieved RAG information.

2. If no sufficiently relevant context exists:
   Do not invent a company policy.

Return an appropriate response indicating that reliable information is unavailable.

3. For dynamic customer-specific information:
   Prefer tool calls instead of LLM-generated facts.

Example:

Refund policy
-> RAG

Order ORD-123 status
-> OrderStatusTool

4. Add confidence/retrieval metadata internally where useful.

5. Create fallback handling for:
   - OpenAI unavailable
   - RAG unavailable
   - Redis unavailable
   - tool unavailable
   - malformed AI output

6. Define graceful-degradation behavior for each failure.

7. Add tests.

Explain why hallucination cannot be completely eliminated and how grounding, tool calling, validation and fallback handling reduce risk.
```

---

# Phase 17 — Correlation ID + Observability

```text
Read the complete application.

Implement Phase 17: Observability.

Goal:
Trace one WhatsApp request through the entire asynchronous pipeline.

Generate/preserve:

correlationId

Flow:

Webhook
 -> Kafka Producer
 -> Kafka Consumer
 -> AIOrchestrator
 -> RAG
 -> LLM
 -> Tool
 -> WhatsApp API

Every important log should include correlationId.

Implement:

1. Correlation ID generation at ingress.

2. Propagation through Kafka event metadata/payload.

3. Logging context/MDC where appropriate.

4. Measure:

webhook processing duration
Kafka processing duration
RAG retrieval duration
LLM duration
tool execution duration
overall processing duration

5. Add Spring Boot Actuator.

6. Expose appropriate health information for:
   - application
   - Redis
   - Kafka
   - database

Do not expose secrets or sensitive configuration.

7. Add basic metrics where practical.

Explain:
- logs
- metrics
- traces
- correlation IDs
- why async Kafka flows make correlation IDs important
```

---

# Phase 18 — Docker Compose + Local Environment

```text
Read the complete project.

Implement Phase 18: Local Docker-based environment.

Create docker-compose.yml for required infrastructure.

Include:

1. Kafka
2. Redis
3. PostgreSQL
4. PGVector if it is being used for RAG

Configure the Spring Boot application so infrastructure locations come from environment variables.

Create:

.env.example

Do NOT put real OpenAI or WhatsApp credentials in the repository.

Provide commands/instructions for:

1. Starting infrastructure.
2. Starting Spring Boot.
3. Loading RAG documents.
4. Sending a simulated webhook.
5. Viewing Kafka processing.
6. Checking Redis conversation data.
7. Checking vector-store data.

Also provide a simple local development profile where WhatsApp outbound calls can be mocked.
```

---

# Phase 19 — Integration Tests

```text
Read the complete application.

Implement Phase 19: Integration testing.

Create tests for the important end-to-end scenarios.

Scenario 1:

Webhook
 -> Kafka
 -> AI processing
 -> mocked LLM
 -> response

Scenario 2:

Question requiring RAG
 -> relevant document retrieval
 -> grounded answer

Scenario 3:

Order question
 -> tool call
 -> structured order result
 -> response

Scenario 4:

Duplicate WhatsApp message
 -> processed once

Scenario 5:

LLM temporary failure
 -> retry
 -> success

Scenario 6:

Repeated failure
 -> DLT

Scenario 7:

Rate limit exceeded

Scenario 8:

Prompt injection attempt

Scenario 9:

Redis conversation memory

Scenario 10:

No relevant RAG information
 -> safe fallback

Use mocks/test containers where appropriate for the current project.

Do not call real OpenAI or WhatsApp APIs from automated tests.

Explain what should be unit tested vs integration tested.
```

---

# Phase 20 — AI Evaluation Framework

This is what you need if you eventually want to defend those **8%, 10%, 15%** improvements in an interview.

```text
Read the completed AI application.

Implement Phase 20: Lightweight AI evaluation framework.

Goal:
Measure RAG quality, response reliability and token usage instead of claiming arbitrary improvements.

Create a small evaluation dataset containing approximately 30-50 questions across:

refund
payment
delivery
orders
general FAQ
out-of-knowledge questions

For each test case store:

question
expectedCategory
expectedRelevantDocument
expectedBehavior

Evaluate:

1. Retrieval relevance.
2. Whether expected documents appear in top-K.
3. Grounded-answer behavior.
4. Tool-selection correctness.
5. Hallucination/fallback behavior.
6. Structured-output validity.
7. Input token usage.
8. Output token usage.
9. Total token usage.
10. Response latency.

Create an evaluation runner.

Produce summary metrics.

Compare configurations such as:

Baseline:
No optimized RAG/context configuration.

Optimized:
- tuned topK
- similarity threshold
- bounded conversation history
- optimized system prompt

Calculate actual percentage differences from measured results.

Do not fabricate improvement percentages.

Output a simple evaluation report.
```

---

## Final Phase — Architecture Review

After everything works, give your coding AI **one final prompt**:

```text
Act as a Senior/Staff Java Engineer reviewing this project before an interview.

Read the complete repository and instructions.md.

Do NOT modify code initially.

Review the implementation and produce:

1. Final HLD.

2. Complete request flow:

WhatsApp
 -> Webhook
 -> Kafka
 -> AI Orchestrator
 -> Guardrails
 -> Memory
 -> RAG
 -> LLM
 -> Tools
 -> Structured Output
 -> WhatsApp

3. Explain every major component.

4. Identify:
   - scalability issues
   - reliability issues
   - security issues
   - AI-specific risks
   - Kafka issues
   - Redis issues
   - RAG weaknesses

5. Explain how the system behaves when:
   - duplicate webhook arrives
   - Kafka consumer crashes
   - OpenAI times out
   - Redis is unavailable
   - vector DB is unavailable
   - tool call fails
   - no RAG document matches
   - malicious prompt is received

6. Identify anything over-engineered for a prototype.

7. Identify anything that would prevent production deployment.

8. Generate the 25 most likely Senior Java/AI Engineer interview questions based specifically on this project.

For every question provide:
- concise interview answer
- likely follow-up question
- strong follow-up answer

9. Give me a 2-minute project explanation suitable for an interview.

10. Give me a 5-minute deep technical explanation suitable for a system-design/techno-managerial round.

Do not invent capabilities that are not actually implemented in the repository.
```

### The architecture you should eventually be able to explain

```text
                        WhatsApp User
                             |
                             v
                     WhatsApp Cloud API
                             |
                             v
                    Webhook Controller
                             |
                    Idempotency Check
                         (Redis)
                             |
                             v
                       Kafka Inbound
                             |
                             v
                       AI Consumer
                             |
                             v
                     Input Guardrails
                             |
                             v
                      AI Orchestrator
                     /       |        \
                    /        |         \
                   v         v          v
              Conversation   RAG       Tools
                Memory     Advisor   OrderStatus
                  |           |
                Redis      Vector DB
                    \        /
                     \      /
                      v    v
                    Context Builder
                          |
                          v
                    Spring AI
                          |
                          v
                       OpenAI
                          |
                          v
                   Output Guardrails
                          |
                          v
                  Structured Response
                          |
                          v
                  WhatsApp Response
```

The most important phases for your **interview story** are **4, 5, 6/7, 8, 11, 12, 14 and 15**. Build those carefully rather than racing through all 20. Those directly let you explain *Spring AI orchestration, memory, RAG/Advisors, tool calling, idempotency, Kafka reliability, guardrails, context engineering and token optimization* instead of only having those terms on the resume.
